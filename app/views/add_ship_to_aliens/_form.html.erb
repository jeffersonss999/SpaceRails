<%= form_with(model: add_ship_to_alien, local: true) do |form| %>
  <% if add_ship_to_alien.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(add_ship_to_alien.errors.count, "error") %> prohibited this add_ship_to_alien from being saved:</h2>

      <ul>
      <% add_ship_to_alien.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ship_id %>
    <%= form.text_field :ship_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
